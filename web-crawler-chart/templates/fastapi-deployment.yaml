apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-fastapi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Chart.Name }}-fastapi
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-fastapi
    spec:
      containers:
        - name: {{ .Chart.Name }}-fastapi
          image: "{{ .Values.image }}:{{ .Values.tag }}"
          ports:
            - containerPort: {{ .Values.port }}
